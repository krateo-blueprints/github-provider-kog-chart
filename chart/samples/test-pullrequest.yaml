apiVersion: github.ogen.krateo.io/v1alpha1
kind: PullRequest
metadata:
  name: test-pullrequest
  namespace: default
  annotations:
    krateo.io/connector-verbose: "true"
spec:
  configurationRef:
    name: my-pullrequest-config
    namespace: default
  owner: krateo-platformops-test
  repo: test-repo
  title: "builder: page example"
  body: |
    Adds the widget CRs for the example page.

    Generated by the portal builder.
  # Resource-side branch names (mapped by the controller into the API `head`/`base` body fields).
  head_ref: builder/page-example
  base_ref: main
  # Uncomment to close the pull request from the CR:
  # state: closed
